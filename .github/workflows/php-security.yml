name: PHP Security

permissions:
  contents: read
  checks: write
  security-events: read

on:
  pull_request: { }
  push:
    branches:
      - master
    tags:
      - v*.*.*

jobs:
  php-security:
    uses: equisoft-actions/php-workflows/.github/workflows/php-security.yml@v9
    secrets: inherit
    with:
      publish-reports: true
      php-version: '8.5'
      extensions: pcov
